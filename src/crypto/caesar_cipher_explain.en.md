# Caesar cipher
A brief summary of the principle of shifting letters by *k* positions in the alphabet.

## Procedure
1. Choose a shift `k`.
2. Replace each letter `x` with `(x + k) mod 26`.

```text
ABC → DEF (k=3)
```